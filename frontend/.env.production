# Replace with the dedicated hosted LMS API before a production deployment.
# Never point this branch at the emji-update desktop backend on port 3001.
VITE_API_URL=https://api.training.example.jp
VITE_API_MODE=production
