# Copy to .env.production before creating a browser deployment build.
# Use the public HTTPS API endpoint. Omit only when the API is served through
# the same origin as the browser application.
VITE_API_URL=https://api.training.example.jp
